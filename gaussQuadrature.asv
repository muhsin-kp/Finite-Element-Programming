function [gaussWeights,gaussPoints]=gaussQuadrature(N)

    if n==2
            gaussPoints=1/sqrt(3)*[ -1 -1
                                        1 -1
                                        1  1
                                       -1  1];
            gaussWeights=[ 1
                    1
                    1
                    1]; 
    else
            gaussPoints=[0 0];
            gaussWeights=4;
    end
end